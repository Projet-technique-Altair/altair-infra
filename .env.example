####################################
# Keycloak — Authentification
####################################

# Compte admin Keycloak (dev uniquement)
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=admin


# URL publique de Keycloak (utilisée par la gateway)
KEYCLOAK_ISSUER=http://keycloak:8080/realms/altair

####################################
# Gateway
####################################

# URL utilisée par le frontend pour appeler la gateway
GATEWAY_BASE_URL=http://localhost:3000

####################################
# Environnement
####################################

# dev | staging | prod
APP_ENV=dev

# Keycloak uses embedded DB (dev-file)
# No external database required